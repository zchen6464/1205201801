package Game;

public class Substringchecker implements Checker
{
  private String goalString;
  public Substrign Checker(String goal)
  {
    goalString = goal;
  }
  
  public boolean accept(String text)
  {
    return (text.indexOf(goalString) != -1);
  }
}
